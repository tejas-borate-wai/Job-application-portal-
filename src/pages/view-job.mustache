<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Apply for {{job.title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .navbar-nav .nav-link {
            background-color: #007bff;
            color: white !important;
            border-radius: 5px;
            padding: 8px 15px;
            transition: 0.3s;
        }

        .navbar-nav .nav-link:hover {
            background-color: #0056b3;
        }

        .job-card {
            border: 1px solid #ddd;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.1);
            background: #f9f9f9;
        }

        .btn-apply {
            background-color: #28a745;
            color: white;
            border-radius: 5px;
            padding: 10px 20px;
            text-decoration: none;
        }

        .btn-apply:hover {
            background-color: #218838;
        }

        .footer {
            text-align: center;
            padding: 15px;
            background: #343a40;
            color: white;
            margin-top: 20px;
        }
    </style>
</head>

<body>

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="#">JobPortal</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item m-2"><a class="nav-link" href="/">Home</a></li>
                    <li class="nav-item m-2"><a class="nav-link" href="/job-list">Jobs</a></li>
                    <li class="nav-item m-2"><a class="nav-link" href="about">About Us</a></li>
                    <li class="nav-item m-2"><a class="nav-link" href="#">Contact Us</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Job Details -->
    <div class="container mt-4">
        <h1 class="text-center">Apply for {{matchedJob.title}}</h1>
        <p class="text-center">You're just one step away from applying for your dream job!</p>

        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="job-card">
                    <h2>{{matchedJob.title}}</h2>
                    <p><strong>Company:</strong> {{matchedJob.company}}</p>
                    <p><strong>Location:</strong> {{matchedJob.location}}</p>
                    <p><strong>Type:</strong> {{matchedJob.type}}</p>
                    <p><strong>Salary:</strong> {{matchedJob.salary}}</p>
                    <p><strong>Description:</strong> {{matchedJob.description}}</p>
                    <p><strong>Requirements:</strong></p>
                    <ul>
                        {{#matchedJob.requirements}}
                        <li>{{.}}</li>
                        {{/matchedJob.requirements}}
                    </ul>
                    <a href="/apply-now/{{matchedJob.id}}" class="btn btn-apply d-block text-center mt-3">Apply Now</a>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <div class="footer">
        &copy; 2025 JobPortal. All rights reserved.
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>